apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: otel-collector-pod-reader-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: otel-collector-pod-reader
subjects:
  - kind: ServiceAccount
    name: otel-collector-nephio-sa # kpt-set: ${appLabel}-sa
    namespace: monitoring # kpt-set: ${namespace}
